{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder:projects}/mycapproject/employee.db",
            "name": "employee.db"
        }
    ]
}